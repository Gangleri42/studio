# Upstream MicroPython is fetched at build time, not vendored.
external/
# Emscripten SDK is installed at build time into emsdk/.
emsdk/
# Bundled assets copied from cmd/coldcard-sim/external/coldcard-photos/ at build time.
assets/coldcard/*.png
# Build outputs.
dist/
node_modules/
coldcard-mpy-*.mjs
coldcard-mpy-*.wasm
