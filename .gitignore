# CI drift-check artifact (regenerated glyphs.js, never committed).
glyphs.gen.js

# Pages build output.
dist/

# Emulator + core build outputs: produced by emu/build.sh and core/build.sh
# and copied to the site root by the Pages deploy (fetched relative to the
# page). Never committed — see emu/.gitignore and core/.gitignore.
/emu.wasm
/studio-core.wasm
/wasm_exec.js

# Local build outputs, never committed.
/coldcard-mpy-*.mjs
/coldcard-mpy-*.wasm

# Cruft.
.DS_Store
__pycache__/
*.pyc

# Local-only dev tree, kept out of the published site.
/coldcard/
/coldcard.js
